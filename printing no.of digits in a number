//  c program to print no.of digits in a number
#include <stdio.h>

int main() {
    int n,count=0;
    printf("enter the value of n:");
    scanf("%d",&n);
    
    while(n!=0){
        n=n/10;
        count++;
        
    }
    printf("no.of digits in number is %d",count);
}
